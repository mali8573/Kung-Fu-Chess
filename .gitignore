# Maven build output (regenerated by `mvn test`/`mvn package`)
target/

# Source-code zip exports (regenerated by scripts/zip-java.ps1|bat)
dist/

# Local player accounts database (created at runtime by GameServer, not shared/committed)
accounts.db

# Console output captured while running the server locally
run-server-out.log
run-server-err.log
